{
  "name": "gastos-pro-server",
  "version": "1.0.0",
  "description": "Backend real de GastosPro: Express + SQLite (node:sqlite) + JWT.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env --watch src/index.js",
    "serve": "node --env-file-if-exists=.env src/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
